<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm border-0 p-4">
        <h2 class="mb-4">Editar Vinho</h2>

        <%# Reaproveite o formulário que você já usa no 'new' %>
        <%= render "form", wine: @wine %>

        <div class="mt-3">
          <%= link_to "Cancelar", wines_path, class: "text-muted" %>
        </div>
      </div>
    </div>
  </div>
</div>